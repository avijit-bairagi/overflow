spring:
  profiles:
    active: avijit
  jpa:
    hibernate:
      use-new-id-generator-mappings: false
      ddl-auto: update
    show-sql: true

server:
  port: 8999
  servlet:
    context-path: /overflow